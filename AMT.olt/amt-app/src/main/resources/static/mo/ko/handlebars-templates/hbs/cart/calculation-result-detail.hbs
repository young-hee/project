<div class="price_box"><span class="price">
	<b>{{numberFormat (calc prodSumAmount '+' shipFeeSumAmount '-' (calc cartEx.calculationResult.totalProdUnitDiscountAmountInfo.standardCurrency.amount '+' totalMemAmount )) }}</b>원</span> <span>{{totalProdCnt}}</span>개 상품
</div>
{{#if (ne cartEx.cartMemberEx null) }}
	{{#if (ne cartEx.calculationResult null)}}
		{{#each cartEx.calculationResult.resultMembershipMap as |value key|}}
			{{#if (eq key ../membershipSn) }}
				<div class="point"><i class="ico_p"></i> <span class="num">{{numberFormat value.totalMembershipSavingPoint }}</span> 적립예정</div>
			{{/if}}
		{{/each}}
	{{/if}}
{{/if}}
