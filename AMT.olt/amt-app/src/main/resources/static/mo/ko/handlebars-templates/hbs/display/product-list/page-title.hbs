<ul class="sub_cate_list">
	{{#each this.submenus}}
		<li>
			<a href="/display/category/{{ this.displayMenuId }}?upperMenuId={{ ../upperMenuId }}">
				<img alt="" src="{{ this.menuTitleImgUrl }}">
				<span>{{ this.menuTitle }}</span>
			</a>
		</li>
	{{/each}}
</ul>
