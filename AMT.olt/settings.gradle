rootProject.name = "AMT"

include "amt-app"

if (System.properties.useEmtIncludeBuild == 'true') {
	includeBuild('../EMT.olt') {
		dependencySubstitution {
			substitute module('kr.ap:ap-comm') with project(':ap-comm')
		}
	}
}