{{#if this.storeExList.length}}
	{{#each this.storeExList}}
		<div class="panel store {{#if (findInObject this.storeEventExList 'foStoreEventCode' 'Color Finder')}}color_find_service{{/if}}">
			<div class="store_wrap">
				<h3 class="store_title">{{ this.storeName }}</h3>
			</div>
			<div class="store_info">
				<p class="store_addr">
					{{ this.address.address1 }}{{#if this.address.address2}}, {{ this.address.address2 }}{{/if}}
					<a href="tel:{{ this.phoneNo1.phoneNo }}" class="store_phone">{{ phoneNumberFormat this.phoneNo1.phoneNo }}</a>
				</p>

				{{!-- open시 해당 영역에 class="on" 이 들어간다. --}}
				<div class="store_btn_wrap">
					{{!-- 단골등록 일때 class="off" 이 들어간다. --}}
					<button class="btn_md w50p map_call">지도보기</button><button class="btn_md w50p del btn_favorite {{#xif this.regularStoreYn '!=' 'Y'}}off{{/xif}}" data-store-sn="{{ this.storeSn }}" data-store-name="{{ this.storeName }}">{{#xif this.regularStoreYn '==' 'Y'}}단골해제{{else}}단골등록{{/xif}}</button>
				</div>

				<div class="store_map_wrap">
					<div class="map_area" data-lat="{{ this.latitude }}" data-lng="{{ this.longitude }}"></div>
				</div>
			</div>
		</div>
	{{/each}}
{{else}}
	<div class="panel notice">
		<i class="ico"></i>
		<p class="color_dark_gray font_lg"></p>
		<p class="text color_gray">매장 검색 결과가 없습니다.</p>
		<p class="text color_light_gray mgt10">입력하신 매장명 또는 지역명을 다시 확인해 주세요.<br>관련 문의는 고객센터 (1544-5418)로 연락주세요.</p>
	</div>
{{/if}}
